# Exemple de variables d'environnement locales
# Copie ce fichier en .env (il est ignoré par git) et remplace les valeurs.

# Hugging Face (recommandé pour éviter les rate limits)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# (Optionnel) Déplacer les caches sur D: si C: est limité
HF_HOME=D:\hf-cache
HF_HUB_CACHE=D:\hf-cache\hub
TRANSFORMERS_CACHE=D:\hf-cache\transformers
HF_DATASETS_CACHE=D:\hf-cache\datasets

# (Optionnel) Réduire la fragmentation mémoire CUDA sur petites VRAM
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64

# API backend
# - stub (défaut) : ne charge pas de modèle (OK pour CI)
# - transformers : charge Qwen + adapter LoRA (GPU recommandé)
TRIAGE_BACKEND=stub

# Backend transformers+PEFT
BASE_MODEL_NAME_OR_PATH=Qwen/Qwen3-1.7B-Base

# Adapter LoRA (chemin local vers un dossier contenant adapter_config.json)
# Exemples:
# ADAPTER_NAME_OR_PATH=checkpoints\qwen3-1.7b-dpo_LONG_20260318_1657
# ADAPTER_NAME_OR_PATH=checkpoints\qwen3-1.7b-dpo_from_sft_lowvram
ADAPTER_NAME_OR_PATH=
